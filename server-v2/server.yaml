# Server Configuration File
server:
  port: 80
  static-dir: ../client/build

logging:
  enabled: true

cache:
  enabled: true
  time_in_hours: 4

lru:
  enabled: true

tls:
  port: 443
  enabled: false
  crt_file: /a/b/c.crt
  key_file: /a/b/c.key

libraries:
  refresh_rate_hours: 1