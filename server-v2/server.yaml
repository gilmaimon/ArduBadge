# Server Configuration File
server:
  port: 80
  static-dir: ../client/build

logging:
  enabled: true

cache:
  enabled: true
  time_in_hours: 4

tls:
  port: 443
  enabled: false
  crt_file: ../cert.pem
  key_file: ../privkey.pem

libraries:
  refresh_rate_hours: 1